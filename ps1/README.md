# README

The scripts in dc_scripts need to be run from the domain controller.

The scripts in rsat_ad_lds can be run from a local host if you have the RSAT: Active Directory Domain Services and Lightweight Directory Services Tools installed.
